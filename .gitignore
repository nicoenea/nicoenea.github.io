# Dependencies
node_modules/
.pnpm-store/

# Build directory
.next/
out/

# Optional: Lockfiles (if you prefer to commit them, you can remove these lines)
package-lock.json
pnpm-lock.yaml
yarn.lock

# Environment variables (IMPORTANT: Keep your secrets safe!)
.env.local
.env.*.local

# Editor/IDE-specific files (Optional, add/remove as needed)
.DS_Store  # macOS Finder
Thumbs.db  # Windows thumbnail cache
.vscode/   # VS Code settings (often better to keep these project-specific)
.idea/     # JetBrains IDEs (like WebStorm)

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage (optional, if you're using test coverage tools)
coverage/
.nyc_output/

# Caching directories (Next.js uses these, but they should be rebuilt)
.cache/
.next/cache/

# other builds
dist/

#vercel builds
.vercel

# amplify builds
.amplify

CLAUDE.md